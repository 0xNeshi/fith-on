import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#947456",
    },
    warning: {
      main: "#569493",
    },
    background: {
      default: "#ACAB71",
      paper: "#949356",
    },
  },
});

export default theme;
